# Plan — Add `scripts.md` + `ai-instructions-modules.md` rules; retire stale `local_ai_instruction_modules/`

**Status:** Implementation-ready
**Target repo:** `intel-agency/agent-context`

## TL;DR

Three coordinated changes:

1. A new `.agents/rules/scripts.md` documenting the repo-root `scripts/` directory from scratch (analyzed from each script's header comments/params per AGENTS.md two-part brief convention).
2. A new `.agents/rules/ai-instructions-modules.md` pointing at `local_ai_instruction_modules/ai-workflow-assignments.md` and `ai-dynamic-workflows.md` (which stay in place for backward-compat with `/commands` and `update-remote-indices.ps1:123-124`) and describing the remote canonical repo `nam20485/agent-instructions`.
3. Salvage two small valid facts from the stale `local_ai_instruction_modules/` modules into existing rules, then `git rm` the four outdated modules.

---

## Tasks (ordered)

### T1 — Create `.agents/rules/scripts.md`

Two-part brief per the AGENTS.md convention (title + summary previewing the 1–2 most important items). Structure:

```markdown
# Repository Scripts

Automation scripts under the repo-root `scripts/` directory — GitHub CLI helpers for auth, label sync, PR review-thread management, permission verification, orchestrator dispatch, and remote-instruction-module index regeneration. All scripts accept a `-DryRun` switch (where applicable) so actions can be previewed without mutation.

<one-line summary of the two most-used ones: query.ps1 and import-labels.ps1>
```

Then a script inventory table **derived from each script's actual header/param block**, NOT copied from the stale `local_ai_instruction_modules/ai-{development-instructions,terminal-commands}.md`:

| Script | Purpose | Key params |
|---|---|---|
| `common-auth.ps1` | Dot-sourceable `gh` auth bootstrap (`Initialize-GitHubAuth`) | `-DryRun` |
| `gh-auth.ps1` | `gh` auth bootstrap with non-interactive PAT-stdin support | `-DryRun`, `-Token <pat>` |
| `import-labels.ps1` | Sync labels from a JSON export (`gh api repos/<owner>/<repo>/labels --paginate > .labels.json`); create/update, optional delete | `-Repo <owner/repo>`, `-LabelsFile ./.labels.json`, `-DryRun`, `-DeleteMissing` |
| `query.ps1` | PR review-thread list/reply/resolve (canonical — do not rewrite ad-hoc) | `-Owner`, `-Repo`, `-PullRequestNumber`, `-ThreadId`, `-Path <wildcard>`, `-BodyContains`, `-Interactive`, `-AutoResolve`, `-NoResolve`, `-DryRun`, `-VerboseLogging`, `-ReplyEach "<msg>"` |
| `create-dispatch-issue.ps1` | Create a dispatch issue on a target repo (title defaults to `orchestrate-dynamic-workflow`) | `-Repo`, `-Title`, `-Body <text>`, `-Labels[]`, `-Project`, `-Milestone`, `-Template`, `-Assignee[]`, `-DryRun` |
| `test-github-permissions.ps1` | End-to-end `gh` auth + scope + repo/project/label/milestone/branch permission verification | `-Owner <user>`, `-TestRepoName`, `-TestProjectName`, `-Cleanup`, `-AutoFixAuth` |
| `update-remote-indices.ps1` | Regenerate the two `local_ai_instruction_modules/` index files from the remote canonical `nam20485/agent-instructions` repo | `-Owner <owner>`, `-Repo <repo>`, `-Branch <branch>` |

Trailing note: `scripts/tmp-issue-body-project-setup.txt` is a leftover temp artifact — candidate for `.gitignore`/deletion (out of scope for this plan; flag to user).

Close with: **"For each script, read the file header comments and `param()` block for authoritative parameter documentation, examples, and `NOTES`. See the `scripts/` directory itself before assuming a script's contract."**

### T2 — Create `.agents/rules/ai-instructions-modules.md`

Two-part brief:

```markdown
# Agent-Instructions Remote Canonical Modules

Index files for workflow assignments and dynamic workflows from the remote canonical repo `nam20485/agent-instructions`. These local index files are the authoritative lookup table for the `/commands` system (orchestrate-dynamic-workflow, orchestrate-project-setup, etc.) and are regenerated by `scripts/update-remote-indices.ps1`.

- Workflow assignments index: `local_ai_instruction_modules/ai-workflow-assignments.md`
- Dynamic workflows index: `local_ai_instruction_modules/ai-dynamic-workflows.md`
```

Then include:
- **What the remote repo is**: `nam20485/agent-instructions` (branch: `main`) — canonical source of workflow-assignment shortIds and dynamic-workflow shortIds.
- **When to use it**: when a workflow needs to resolve a shortId; the `/commands` resolve assignments via the local index file, which is kept in sync with the remote by `update-remote-indices.ps1`.
- **Where things live**: both index files stay at `local_ai_instruction_modules/` — **do not relocate**; paths are hard-coded in `/commands` and in `scripts/update-remote-indices.ps1:123-124`.
- **How to refresh indices**: run `pwsh ./scripts/update-remote-indices.ps1` (defaults to `nam20485/agent-instructions` @ `main`).

### T3 — Salvage SHA-Pinned GitHub Actions rule into `ci-cd.md`

Append a `## GitHub Actions (SHA-Pinned — MANDATORY)` section:

```markdown
## GitHub Actions — SHA-Pinned (MANDATORY)

Every `uses:` line in workflow files **MUST** reference the full 40-char commit SHA of the targeted release; tag refs (`@v4`, `@main`) are **prohibited** — mutable tags are a supply-chain attack vector.

- Format: `uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2`
- Trailing `# vX.Y.Z` comment is mandatory for readability.
- Applies to all actions: third-party, `actions/*`, `github/*`, reusable workflows.
- Not enforced by `actionlint` — enforced by review.
```

**Rationale**: current `ci-cd.md:13-16` only says "always use very specific version pinning" and does NOT spell out the 40-char SHA or the trailing comment. The `ai-development-instructions.md:70-77` content is still valid and applicable specifically to GitHub Actions.

### T4 — Salvage CLI error-handling triage into `practices.md`

Append a `## Handling Command Failures` under the existing `## Investigation` section (after line 41):

```markdown
## Handling Command Failures

When a CLI command fails, before retrying:

1. Read the error message and its context for hints.
2. Verify command syntax (`--help`, `Get-Help`, tool docs).
3. Inspect usage examples; construct a correct command from careful analysis.
4. For complex commands, break them into smaller parts and test each.

**Do not keep retrying the same command without understanding the issue.** If the command still fails after up to 3 informed attempts, search the web for the error message / docs before retrying again. Once a working command is found, document it where appropriate for future runs.

Exit-code checks: `$?` (bash success/fail) or `$LASTEXITCODE` (pwsh / native command exit code).
```

**Rationale**: current `practices.md:36-41` says "never guess / investigate root cause" but lacks the structured triage pattern. The `ai-terminal-commands.md:60-72` content is still valid general practice, not specific to the obsolete module.

### T5 — Delete stale `local_ai_instruction_modules/` files

`git rm` each and commit in a dedicated commit (one commit for clarity):

- `local_ai_instruction_modules/ai-custom-agents.md`
- `local_ai_instruction_modules/ai-delegation-mandate.md`
- `local_ai_instruction_modules/ai-development-instructions.md`
- `local_ai_instruction_modules/ai-terminal-commands.md`

**Do NOT touch these** (they stay in place for `/commands` + `update-remote-indices.ps1`):
- `local_ai_instruction_modules/ai-workflow-assignments.md`
- `local_ai_instruction_modules/ai-dynamic-workflows.md`

### T6 — Update `AGENTS.md` to reference new rules

Add two brief entries to the Rules list (`AGENTS.md:20-32`). Each must follow the two-part brief convention (title describing content type + summary previewing the 1–2 most important items):

```markdown
- **Scripts**: [`.agents/rules/scripts.md`](.agents/rules/scripts.md) — repo-root `scripts/` inventory (auth helpers, `import-labels.ps1`, `query.ps1` for PR review-thread management, dispatch-issue creator, permission verifier, remote-index regenerator). Read each script's header + `param()` block for authoritative docs.
- **Agent-Instructions Modules**: [`.agents/rules/ai-instructions-modules.md`](.agents/rules/ai-instructions-modules.md) — lookup tables for `nam20485/agent-instructions` workflow assignments and dynamic workflows, stored at `local_ai_instruction_modules/` (hard-coded path; refreshed by `scripts/update-remote-indices.ps1`).
```

### T7 — Validation

1. Syntax: `pwsh -NoProfile -Command "... [PSParser]::Tokenize ..."` for any changed `.ps1` (none are changed in this plan, but re-run if the scripts.md draft quotes real commands).
2. Markdown: `npx --no-install markdownlint-cli2 .agents/rules/scripts.md .agents/rules/ai-instructions-modules.md .agents/rules/ci-cd.md .agents/rules/practices.md AGENTS.md`.
3. Secret scan: `bash ~/.agents/skills/scan-uncommitted-secrets/scripts/scan.sh` before each commit.

### T8 — Memory update

Append under `.agents/memory.md` **Decisions**:

> **Rules restructuring (YYYY-MM-DD)**: Consolidated legacy `local_ai_instruction_modules/` — added `.agents/rules/scripts.md` (repo-root scripts brief) and `.agents/rules/ai-instructions-modules.md` (remote canonical repo pointer). Deleted four stale/contradictory modules (`ai-custom-agents.md`, `ai-delegation-mandate.md`, `ai-development-instructions.md`, `ai-terminal-commands.md`); kept the two index files in place because `/commands` and `scripts/update-remote-indices.ps1:123-124` hard-code that path. Salvaged SHA-Pinned Actions rule → `ci-cd.md`; CLI error-triage pattern → `practices.md`.

---

## Commit plan (two commits for clarity)

1. `docs(rules): add scripts.md and ai-instructions-modules.md; add SHA-pinning to ci-cd.md; add CLI triage to practices.md; update AGENTS.md briefs` (adds T1, T2, T3, T4, T6, T8).
2. `docs: retire stale local_ai_instruction_modules (custom-agents, delegation-mandate, development-instructions, terminal-commands)` (T5).

---

## Risks

- **Backward-compat for `/commands`**: mitigated by explicitly preserving both index files at `local_ai_instruction_modules/`. Confirmed by reading `update-remote-indices.ps1:123-124`.
- **`/commands` that may have quoted content inside the deleted modules**: not in scope (the legacy module content is the lookup table — those remain). If any `/command` references prose in the deleted files, that needs a separate fix; this plan does not audit `/command` implementations.
- **`tmp-issue-body-project-setup.txt`**: not removed in this plan (it's a separate cleanup). Called out to user for decision.

---

## Out of scope

- Moving any of `local_ai_instruction_modules/` content under `.agents/rules/` wholesale (backward-compat constraint).
- Cleaning up `scripts/tmp-issue-body-project-setup.txt`.
- Auditing `/commands` implementations for any prose references to the deleted modules.
- Changes to `.agents/skills/gh-issue-tracking-init/` (separate skill, self-contained).
