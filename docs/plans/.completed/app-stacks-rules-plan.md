# App Stacks Rules

@.agents/rules/app-stacks App stacks are ready-made pre-defined application implementation language & stack definitions. They can be referred to in app development / implementation plans to specify the language, tech stack, tools, and packages to use for the implementation. Each app stack is specified in a file named by the app stack "slug" id under @.agents/rules/app-stacks dir

Add a section to @AGENTS.md explaining their use and where to find them.

Questions:

1. do we need an actual file `.agents/rules/app-stacks.md` to explain them, or just reference the dir directly in @AGENTS.md ?

2. They will be dynamic, so it may be more efficient to specify the dir and explain the naming rather than list them all, or list them all since there are only three so far?
