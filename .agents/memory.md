# Project Memory

## Current Activity

Current project and its sub-work items that we are working on actively. This section is a placeholder for the current project and its work items, which will be updated as we progress. Once completed, the work items will be moved to the "Completed Work Items" section below.

### Project: Agent-readiness hardening

- **Root README.md** (2026-07-24): Created `README.md` at repo root with repository identity, prerequisites (`pwsh` 7+, `gh` with `repo`/`project`/`user:email` scopes), getting-started steps, configuration pointer to `docs/environment-variables.md`, repository layout table, Pester test command (`Invoke-Pester -Path .agents/skills/gh-issue-tracking-init/scripts/tests -Output Detailed`, 101 tests), linting instructions, and contributing guidelines. Closes readiness criterion `readme`.
- **Label taxonomy import** (2026-07-24): Imported the canonical labels from `.agents/skills/gh-issue-tracking-init/assets/labels.json` into the GitHub repository via `pwsh .agents/skills/gh-issue-tracking-init/scripts/ensure-labels.ps1 -Repo intel-agency/agent-context`. Canonical set (19): `plan`/`epic`/`story`/`task`, `P0`-`P3`, `area/ai|ui|core|infra|docs`, `blocked`, `needs-review`, `wontfix`, `gh-issue-tracking:direct-body`, `gh-issue-tracking:init-success`, `gh-issue-tracking:epic-implemented` (added via PR #16). Default GitHub labels left intact (no `-DeleteMissing`). Closes readiness criterion `issue_labeling_system`.
- **Branch protection ruleset** (2026-07-24): Created GitHub ruleset `protect-development-and-main` covering `refs/heads/development` and `refs/heads/main` with `deletion`, `non_fast_forward`, and `pull_request` (1 approving review, required review thread resolution) rules. Admin bypass enabled (`RepositoryRole` id 5, `always` mode) because GitHub forbids self-approval and `nam20485` is the sole reviewer; a no-bypass 1-approval rule would deadlock every PR. Closes readiness criterion `branch_protection`.
- **`.github/` directory + CI pipeline** (2026-07-24): Created `.github/CODEOWNERS`, `pull_request_template.md`, 3 issue form templates (bug/feature/task), `.github/workflows/ci.yml` (SHA-pinned actions, runs `validation.ps1` on push + PR), root `validation.ps1` (build/scan/test with >85% coverage gate + JaCoCo + ReportGenerator HTML artifact), `.gitleaks.toml` (allowlists fake-secret test fixtures), `.markdownlint-cli2.jsonc` (scoped lint globs for docs-of-record). Closes `codeowners`, `issue_templates`, `pr_templates`, `fast_ci_feedback`, `test_performance_tracking`, `agents_md_validation`, `build_performance_tracking`, `gitignore_comprehensive`. (CODEOWNERS and issue form templates subsequently removed — see Decisions below.)
- **Coverage raised 44.85% to 93.91%** (2026-07-24): 3 parallel worker subagents wrote 89 new Pester tests across 3 new test files (`ImportLabels.Tests.ps1`, `MilestonesAndLinks.Tests.ps1`, `EnsureIssueAndProject.Tests.ps1`). Total: 190 tests, 0 failures, 802/854 commands covered. All 11 skill scripts now exceed 85% individually.
- **`.gitignore` expanded** (2026-07-24): Added `.env`/`.env.*`/`!.env.example`, `node_modules/`, build artifacts (`bin/`/`obj/`/`dist/`/`build/`/`out/`), coverage artifacts, IDE configs (`.idea/`/`.vs/`), OS files (`.DS_Store`/`Thumbs.db`).
- **Markdownlint fixes** (2026-07-24): Fixed MD040 (missing fence language in `coding-style.md`) and MD034 (bare URL in `skills.md`). Created `.markdownlint-cli2.jsonc` with scoped globs (`README.md`, `AGENTS.md`, `.agents/memory.md`, `.agents/rules/**/*.md`, `docs/*.md`) to exclude intentional placeholder syntax in plan/issue templates.

### Project: Template-repo content strategy

- **`gh-issue-tracking-init` no-arg defaults** (2026-07-18): Made defaults explicit in the skill — `$ghrepo` defaults to the current repo (`gh repo view --json nameWithOwner`); plan source resolves **non-interactively** from `plan_docs/` by filename role (primary/architecture/reference), with an empty/missing `plan_docs/` as the only hard stop. Updated `SKILL.md` (Inputs + orchestration preface) and `README.md`. The earlier "defaults to all docs / confirm with user" wording was a defect (prompted interactively in downstream test); replaced by the deterministic resolver — see completed item below.
- **Template vs. clone content strategy** (2026-07-18, revised 2026-07-19): Authored [`docs/plans/.deferred/template-content-strategy.md`](../docs/plans/.deferred/template-content-strategy.md) — two-class model (Class 1 reusable infra travels; Class 2 template-self-referential state must be reset on clone / filtered on back-flow). Work items: W1 post-clone reset step, W2 back-flow discipline, W3 marker convention (RESOLVED 2026-07-19 — well-known paths suffice). **2026-07-19 revision** analyzed the external cloning pipeline (`nam20485/workflow-launch2` scripts `create-repo-with-plan-docs.ps1` + `trigger-project-setup.ps1`) and verified against a real clone (`intel-agency/gap-miner-v2-delta12`): W1 step 4 (seed plan docs) is handled; **W1 step 5 (build hierarchy) trigger is broken** (dispatches the incompatible `/orchestrate-dynamic-workflow project-setup` — needs replacement with `/gh-issue-tracking-init` direct dispatch); W1 steps 1–3 (memory reset, clear template plans, remove foreign artifacts) are NOT handled; **AGENTS.md anchor bug** — script targeted `**GitHub template repo**` but template said `**upstream GitHub template**` → **W1.4 FIXED 2026-07-19 (option a)**: reworded template AGENTS.md first sentence to contain the anchor; apposition now reads correctly in both template and clone contexts. W1.1–W1.3 + W1.5 remain pending in the external `workflow-launch2` repo.

#### Work Items

### Project: Agent swarm (ZCode harness)

- **`AGENT_SWARM_PLAN.md` review** (2026-09-05): Plan review written to [`docs/plans/AGENT_SWARM_PLAN-review.md`](../docs/plans/AGENT_SWARM_PLAN-review.md) — findings scored value÷cost into 3 buckets. Bucket A (apply pre-implementation): `finish` guard must be `endedAt`-based (status-based reading contradicts the plan's own E2E), add a resume path to SKILL.md (compaction claim has no consumer), document per-round-only budget enforcement, Windows/git symlink contingency for template clones, fix `.agents/rules/skills.md` validator command (`skills-ref` doesn't exist; `uvx --from skills-ref agentskills validate` verified), worker-body line for AGENTS.md-injected tools they lack, 4 missing unit tests, gate PR on GUI smoke. Verified correct: depth-1 constraint, frontmatter keys, tools-list exhaustiveness, symlink discovery (empirical), `validation.ps1:143-150` anchors.

- **Swarm plan user directives** (2026-09-06, fold into `AGENT_SWARM_PLAN.md` at approval; recorded in the review doc's "User-directed updates" section, superseding its items 6 and 11): (1) spawn all swarm workers as **background** agents — foreground batches block the primary session for the batch lifetime (docs-verified `#background`); (2) **`injectAgentsMd: false`** on all six subagent defs + shared worker-instructions file `.agents/rules/swarm-workers.md` loaded via read-first markdown-link directive in each worker body (subagent definitions have no include mechanism — docs-verified; the delegation contract's Constraints element becomes the workers' only channel to repo rules); (3) **no interactive permission prompts** — docs-verified that "Edit automatically" still confirms every command and permission requests wait indefinitely (no auto-continue; no config-file permission key exists) → SKILL.md needs a Full-access/Always-Allow preflight before `init`, orchestrator treats a permission-gated worker as a stall (TaskStop + failed), and the smoke's "mode = Edit automatically" must become "Full access".

- **Goal Mode finding** (2026-09-06): ZCode's `/goal` (<https://zcode.z.ai/en/docs/goal>) is a native objective loop that mirrors the swarm plan's outer loop — harness-driven rounds, next-step chaining, persisted goal state, per-goal usage budget — and its round-end **separate harness-side verification** uses an evidence standard verbatim-equivalent to the plan's (changed files/command output/test results count; plans/effort/conclusive-sounding replies don't). Bears on two plan weaknesses: orchestrator self-graded verdicts and self-driven continuation. Constraints: user-only invocation (no agent/skill can set a goal), can't set while a task runs, stopping a task auto-pauses the goal, subagent/background composition undocumented. **User rejected Goal Mode as a substrate (2026-09-06)**; the two weaknesses got in-plan solutions instead — review item 19 (gate `end-round -Verdict met` on a cold `swarm-verifier` PASS; orchestrator records verdicts, never invents them) and item 2's re-entry-contract extension (first action on any re-invocation = `swarm-state.ps1 status`; continuation event-driven via U1 pending-task records). Optional `/goal` insurance line + smoke probe remain in item 18.

- **Review decisions folded into `AGENT_SWARM_PLAN.md`** (2026-09-06): U1–U3 applied as written (background workers with pending-at-spawn task records; `injectAgentsMd: false` + `.agents/rules/swarm-workers.md` read-first line; Full-access/Always-Allow preflight before `init`). Items without FEEDBACK accepted as recommended: 1 (`finish` guard = `endedAt`, ValidateSet stops-only, never finish after met), 2 (skill resume path + orchestrator/SKILL re-entry contract), 3 (per-round budget check by orchestrator), 5 (skills.md validator snippet → `uvx --from skills-ref`), 7 (four added Pester cases), 10 (skill self-containment exception), 12 (`-Next` optional on met), 15–17 (citation reword, FAQ cites → empirical anchors, newline sanitize + serialization pin), 19 (cold-verifier verdict gate + smoke hardening). FEEDBACK directions applied: **no symlinks** (items 4/8 — definitions are real files in `.zcode/agents/`, skill at `.agents/skills/swarm/`, symlink step deleted, `.gitignore` simplified, cross-harness deferred), item 6 (subsumed by U2), item 9 (keep `$swarm` native trigger, `/` auto-converts), item 13 (verifier/reviewer stay separate), item 14 (dependency wording fixed). Plan renumbered to 6 steps; smoke now requires Full access + a required cold `swarm-verifier` PASS grounding the final `met`, with optional permission and `/goal` probes.

- **Swarm implementation delivered via PR #1** (2026-09-06, branch `dev/agent-swarm`, milestone `agent-swarm`, project "Agent As A Service"): all 6 plan commits pushed — plan fold (`732b8e9`), state script + tests + validation wiring (`88457da`), definitions + skill + rules (`5e26947`), gitignore + memory (`beb5342`), plus two droid-workflow hardening commits (`5ece92f`→`1dde2eb`→`f9c0d48`). Full gate green at each commit (231/231 tests, 94.40% coverage). PR checks green (`validate` + `droid-review` skip-clean). **CI lessons (2026-09-06):** (1) `droid-review` failed fast on this repo's first PR because `secrets.FACTORY_API_KEY` was never configured — the repo needs the secret added (Settings → Secrets → Actions) to get real Droid reviews; until then the workflows skip cleanly. (2) **The `secrets` context is unavailable in `if` conditionals at job OR step level** — GitHub rejects the whole workflow file at startup (phantom 0s `push`-event failures on both droid workflows even though neither declares a push trigger). Supported pattern: map the secret to `env:` and test `env.<VAR> != ''` in the step `if`; `actionlint` (at `~/.local/bin`) catches this pre-push. Remaining for PR #1: user GUI smoke (checklist in the PR body), review, merge.

### Project: Rules upkeep

- **PowerShell Engineer Standard rules hierarchy** (2026-08-15; landed 2026-08-18): Plan at `docs/plans/powershell-standard-rules-plan.md` — **Option A+ decided** (user, 2026-08-15): vendored hierarchy at `.agents/rules/powershell/` generated by the **skill** `.agents/skills/update-powershell-standard/` (check-upstream + fetch + re-split; script lives inside the skill per self-containment; upstream itself is the source of truth; topic files are generated, never hand-edited; house content lives only in the hand-maintained `.agents/rules/powershell.md` index). Plus a planned `powershell-script-expert` subagent for **both** `.kilo/agent/` and `.opencode/agents/` (YAML-frontmatter format like the existing eight; write/plan/review/debug/research + corpus refresh via the skill; delegate non-trivial PS work only). Upstream publishes full markdown at `https://www.powershellengineer.com/AGENTS.md` (v1.1.1, MIT) — no HTML conversion. **Status:** plan step 1 partially landed in PR #17 (commit `d253687`: the split/refresh script; commit `a92b892`-era follow-ups added `SKILL.md`, Pester suite at 96% coverage, and `validation.ps1` registration). Note: commit `a84e5e1`'s title says "and subagent definition" but it touched only `memory.md` — no subagent definition files exist yet (`.kilo/agent/` does not exist). Remaining: steps 2–7 (hand-maintained index, subagent definitions, AGENTS.md/delegation.md updates, house overrides, cleanup of the planning snapshot).
- **Pester 5 gotcha rules added** (2026-08-18): Two silently-wrong-test pitfalls found while writing the `update-powershell-standard` Pester suite (PR #17 round-4) are now codified in `.agents/rules/validation.md` § "Pester 5 gotchas (repo-verified)" with an AGENTS.md Validation-section brief: (1) a second `BeforeAll` in one `Describe` breaks `$script:` visibility — every dependent `It` sees `$null`; (2) backtick fence lines inside a double-quoted here-string `@"…"@` get escape-mangled (`` `` `` → one backtick, trailing backtick + newline → line continuation) — use `@'…'@`. Repo audited clean (AST-based `BeforeAll` scan + here-string fence scan over all `.ps1`); auditors verified against a synthetic bad file before trusting the clean result.
- **PR #17 round-5 review resolution** (2026-08-19, commits `eb4576f`–`5654457`): Resolved all 5 unresolved review threads. (1) label-import note now records the full relative invocation (`pwsh .agents/skills/gh-issue-tracking-init/scripts/ensure-labels.ps1 -Repo intel-agency/agent-context`); (2) `powershell-standard-rules-plan.md` aligned with the implemented contract — 11 topic files, with *Definition of done*/*Lineage*/*Deeper guidance* folding into `99-definition-of-done.md` (mapping-table row added, step 1 reworded); (3) plan step 7 rewritten — the planning snapshot `docs/plans/powershellengineers_AGENTS.md` was never committed, so cleanup needs no deletion; (4) `update-powershell-standard.ps1` CheckOnly/Refreshed summaries now report `UpdateAvailable` from `$updateAvailable` instead of hard-coded `$true` (two new Pester assertions; suite 16/16, `validation.ps1` green at 94.32% coverage); (5) `.opencode/agents/orchestrator.md` gh allow-list tightened from `gh pr*/issue*/run*` wildcards to explicit read-only subcommands — the same wildcard pattern remains in 5 other `.opencode/agents/` files (follow-up candidate).
- **Z.AI MCP tool reference expanded** (2026-08-30): Restructured `.agents/rules/tools.md` § "Web & Repository Research (Z.AI MCP)" into per-server subsections (`web-search-prime`, `web-reader`, `zread`), each with its Features, tool list/description, and Example Scenarios distilled from the Z.AI DevPack doc pages (overview → Example Scenarios sections only; installation/troubleshooting skipped). Routing decision points and params were already documented — Features cards + scenarios were the net-new content. Section now also notes the three servers are the ones configured in `.opencode/opencode.jsonc`. **Source-format gotcha:** the docs' Features sections are Mintlify card components — the HTML pages drop card bodies from `webReader` extraction while the raw `<page>.md` exports keep them, and the reverse for some Example Scenarios bodies; when distilling these pages, fetch **both** forms and merge. **User validation** (2026-08-30): models use these three servers constantly and have run with noticeably fewer tool errors/retries since the expanded docs landed — treat detailed tool documentation in rules as high-value, not bloat.
- **Task-input structure rule** (2026-08-30): Added "Structuring Task Inputs" section to `.agents/rules/delegation.md` — four-element task-input template (Goal / Context / Constraints / Done when) for any delegation or orchestration dispatch, distilled from [Z.AI DevPack best practices](https://docs.z.ai/devpack/resources/best-practice#2-structure-task-inputs-context-matters-more-than-prompting-tricks). Rationale: a delegate starts cold; existing rules covered who to delegate to and which layer, but not how to compose the dispatch. AGENTS.md Delegation & Orchestration brief updated to preview the four elements. Deliberately not redundant with Goal-Driven Execution or Orientation — both are executor-side; this rule is dispatcher-side.
- **Kilo Code docs rule** (2026-08-14): Created `.agents/rules/kilo-code-docs.md` — mandates answering any Kilo Code CLI/IDE-extension question or setup/config instruction from the official docs via `https://kilo.ai/docs/llms.txt` (full LLM dump) or `https://kilo.ai/docs/api/raw-markdown?path=<url-encoded-path>` (per-page raw Markdown, no `/docs` prefix), instead of from memory. Distilled from `docs/using-docs-with-agents.md`; AGENTS.md Rules list updated with the entry. Source doc subsequently lint-fixed (removed duplicate YAML `title:`, added `text` fence languages) so it passes CI lint if committed.
- **Qwen Code docs rule** (2026-09-01): Created `.agents/rules/qwen-code-docs.md` — same consult-the-docs mandate as the Kilo rule, for Qwen Code usage/features/config/troubleshooting questions. Access points (all fetch-verified): markdown index `https://qwenlm.github.io/qwen-code-docs/llms.txt` (index of links + descriptions only — **no** `llms-full.txt` full dump exists, unlike Kilo) and per-page raw Markdown from the upstream source repo `https://raw.githubusercontent.com/QwenLM/qwen-code/main/docs/<path>.md`, where `<path>` = site URL path minus the `/en/` prefix and trailing slash plus `.md` (e.g. `/en/users/features/mcp/` → `docs/users/features/mcp.md`). Raw-path 404 fallback: fetch the rendered site page. AGENTS.md Rules list updated with the entry.

## Completed Work Items

### Project: Agent-readiness hardening (cont.)

- **PR #15 round-3 review resolution** (2026-07-30, commits `07a2bab` + `8235628`): Resolved all 3 remaining unresolved review threads on PR #15 (`nam/alpha61-run-report-fixes`). (1) Stale-docs thread — corrected references in `memory.md` and `template-content-strategy.md` to the deleted `.github/CODEOWNERS` + issue form templates, added a Decision entry recording the removal (commit `ea18c8e`); (2) orchestrator web-access thread — no code change, resolved as an accepted-by-design user decision (commit `131ac5a`); (3) MCP supply-chain thread — pinned both local MCP servers to `@2026.7.4` (verified npm `latest`). Result: 8/8 threads resolved, 0 unresolved.

### Project: gh-issue-tracking-init plan-source auto-resolution (2026-07-18)

Applied the plan documented in [`docs/plans/.completed/gh-issue-tracking-plan-source-resolution.md`](../docs/plans/.completed/gh-issue-tracking-plan-source-resolution.md). Downstream template-clone test revealed the skill was stopping to ask "which plan doc?" between a development plan and a reference doc — a defect for a non-interactive skill. Fix (commits `9a0cf01` + `1e516a8`): replaced the "confirm the selection (or merge) with the user" default in `SKILL.md` Inputs and the "ask before doing anything" orchestration preface with a deterministic filename-role resolver (primary → issue tree; architecture/reference → Plan-body context; empty `plan_docs/` is the only hard stop). Mirrored in `README.md`. Prose-only; Pester 46/46 green; both edited files markdownlint-clean.

### Project: gh-issue-tracking-init driver fixes + Gap Mining hierarchy recovery (2026-07-16)

Implemented `docs/plans/gh-issue-tracking/run-issues-review/gh-issue-tracking-fix-trace.md` against `intel-agency/gap-miner-v2-oscar32` (Project #88). Driver: `/tmp/kilo/gh-init-driver.ps1`.

- **Driver fixes (all tasks T1–T8):** trace logging (`Trace-Write`/`Invoke-ScriptWithTrace`, per-run file `gh-init-trace-*.log`); renamed `$num`/`$Num` → `$issueNum`/`$NumberByKey` (case-collision root cause); normalized all epics to `Parent='P'` and every story to `Level='story'`; pre-flight schema check (Level/Title/Parent/AC); DryRun body-generation parity; post-run verification block. Added `-ProjectNumber` and `-SkipFields` recovery switches.
- **Skill fixes discovered in-run** (`.agents/skills/gh-issue-tracking-init/scripts/common.ps1`): (1) `Get-IssueDbId` cast changed `[int]`→`[long]` — GitHub issue DB IDs now exceed Int32.MaxValue, breaking sub-issue linking + dependencies; (2) `Find-IssueNumberByTitle` rewritten to use the REST issues endpoint (paginated) instead of `gh issue list --search`, which routes through the GraphQL Search API and fails under its separate rate limit.
- **Result:** hierarchy complete and verified in a clean trace (0 ERR): 30 issues, Plan #1 → 7 epic sub-issues, each epic → its stories (22 total), board = 30 items, 32 dependency edges. `ALL COUNTS MATCH`.
- **Operational note:** heavy GraphQL usage (Projects v2 field stage) exhausts the 5000-point/hour GraphQL budget separately from the REST core limit; recovery switches let re-runs proceed REST-only while GraphQL resets.

### Project: Rules and Memory Consolidation

- **App Stacks section added** (2026-07-15): Added an `App Stacks` entry to the AGENTS.md Rules list, referencing `.agents/rules/app-stacks/` as the home for pre-defined language/tech stack profiles named by slug ID. All 3 existing stacks listed inline. Fixed pre-existing issues: renamed `dotnet-avalonia-xplatform-desktop` to add `.md` extension, corrected wrong H1 (`# python-uv-fastapi-vite` → `# dotnet-aspire-aspnet-blazor`) in the Aspire stack file. Decided against a separate `app-stacks.md` rules file — the directory is self-describing.
- **Tool rules consolidation** (2026-07-10): Moved all tool-related information (Sequential-Thinking, Memory, Semantic Search, Z.AI MCP, Exa MCP) from `AGENTS.md` and the two `docs/tool-*.md` guides into a single new `.agents/rules/tools.md`. AGENTS.md `## Tool Usage` section is now a 2-line pointer; `docs/tool-memory.md` and `docs/tool-sequential-thinking.md` were deleted (content merged, not lost). AGENTS.md rules list now includes a `Tools` entry.
- **Additional rules file extraction** (2026-07-10): Created three more rules files from AGENTS.md content — `.agents/rules/delegation.md` (Delegation + Orchestration), `.agents/rules/validation.md` (Validation + Testing + TDD), `.agents/rules/source-control.md` (Committing/Safe Commit/Monitor/Branching/PRs). Each AGENTS.md section slimmed to a brief pointer. Rules list updated: added `Delegation` entry, removed `Testing` entry (merged into validation.md). CI and Scripts entries remain stub references (files not yet created).
- **Practices rules file + relocation directive** (2026-07-10): Combined the remaining three AGENTS.md sections (Planning, Investigation, Making Changes) into `.agents/rules/practices.md`, framed as the 3-stage engineering lifecycle. Added a written directive to AGENTS.md `#### Files` section: when relocating content to a rules file, the section brief's heading/summary must describe what the reader finds, not just name the subject. All AGENTS.md content sections now follow the brief-with-link pattern.
- **Memory and Rules section cleanup** (2026-07-10): Condensed the AGENTS.md `## Memory and Rules` section from 75 to 32 lines. Removed redundant "consult before starting" (was 3×) and "update as you go" (was 3×) restatements. Removed stale Rules#Examples list (had duplicate "validation" entry) and Memory#Examples subsection. Removed non-existent `ci.md` and `scripts.md` from rules file list. Fixed typos ("informatin", "assumptions-", double "is"). Concentrated uppercase/bold emphasis on 3 distinct critical directives. Full file is now 60 lines.

## Decisions

- **Removed `.github/CODEOWNERS` and issue form templates** (2026-07-30, commit `ea18c8e`): Deleted `.github/CODEOWNERS` and the issue form templates (`bug_report.yml`, `feature_request.yml`, `task.yml`, `config.yml`) in the `droid-wiki` cleanup commit. CODEOWNERS was a single-owner repo where the sole maintainer was also the only code owner, adding no review value. Issue form templates were replaced by the gh-issue-tracking-init skill's programmatic issue creation, which uses the GitHub API directly and does not need form templates.

- **Branch protection: admin bypass over no-bypass** (2026-07-24): The readiness report recommended requiring >=1 approving review on `development` and `main`. GitHub forbids self-approval, and `nam20485` is the sole maintainer/admin. A no-bypass 1-approval rule would make every PR unmergeable. Chose Option A: ruleset with `pull_request` (1 approval) + `deletion` + `non_fast_forward`, with admin bypass actor (`RepositoryRole` id 5, `always`). Agents and non-admin tokens must still open PRs and get reviews; the admin retains the ability to push/merge directly when needed.

- **Coverage gate scope: entire scripts dir (Option C)** (2026-07-24): `ci-cd.md` mandates >85% coverage. Three scope options were considered: A (two fully-tested files only, 93%), B (all 3 tested files, add ~5 tests to hit 87%), C (entire scripts dir, write ~60 tests to reach 85%). Chose C because it honestly reflects the full codebase and doesn't game the gate by excluding under-tested files. Result: 93.91% (802/854) across all 11 skill scripts via 89 new tests from 3 parallel worker subagents.

- **Gitleaks binary over gitleaks-action** (2026-07-24): The CI workflow uses the gitleaks binary (downloaded directly) instead of `gitleaks/gitleaks-action` because the action requires a paid `GITLEAKS_LICENSE` for organization repos like `intel-agency`. The binary is free and functionally identical for `detect` mode. A `.gitleaks.toml` config allowlists the two intentional fake-secret fixtures in `AssertNoSecrets.Tests.ps1`.

- **Rules restructuring of `local_ai_instruction_modules/`** (2026-07-18): Consolidated the legacy module directory. Added `.agents/rules/scripts.md` (repo-root `scripts/` brief, built from each script's header/`param()` block — fills the long-standing stub referenced in the 2026-07-10 notes) and `.agents/rules/ai-instructions-modules.md` (pointer to the remote canonical repo `nam20485/agent-instructions`). Deleted four stale/contradictory modules — `ai-custom-agents.md` (25-subagent taxonomy, conflicts with `delegation.md`), `ai-delegation-mandate.md` (75% coverage / FORBIDDEN direct execution, conflicts with `delegation.md`), `ai-development-instructions.md` (references nonexistent `AGENTS.md mandatory_tool_protocols`, stale `mcp_*` tooling), `ai-terminal-commands.md`. Kept `ai-workflow-assignments.md` and `ai-dynamic-workflows.md` in place — their path is hard-coded by `scripts/update-remote-indices.ps1:123-124` and the `/commands` that resolve shortIds. Salvaged two valid facts: SHA-Pinned Actions rule → `ci-cd.md`; CLI error-triage pattern → `practices.md`.

## Remember To Do

Things to plan, add, or change when we are done with the current activity.
